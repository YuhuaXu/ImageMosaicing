#pragma once

//#include "atlimage.h"

#include "Bitmap.h"

//int CreateATLImage(const pool::BitmapImage* pSrcImage, ATL::CImage* &pAtlImage);

//bool SaveImageATL(const pool::BitmapImage *pSrc, TCHAR * pPath);

//bool SaveImageATL(const pool::BitmapImage *pSrc, char * pPath);
